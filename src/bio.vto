---
layout: layouts/terms.vto
id: bio
title: メンバー紹介
metas:
  title: メンバー紹介
  description: asdf
---

<ul>
  {{ for item of bios }}
  <li>
    <article>
      <img src="{{ item.img }}" alt="{{ item.name }}" width="200px"><br>
      <strong>{{ item.name }}</strong>
      <p>{{ item.title }}</p>
      <p>{{ item.short }}</p>
    </article>
  </li>
  {{ /for }}
</ul>
