---
layout: ./base.vto
containerClass: ceo
---
<section class="ceo" id="ceo">
  <div class="inner">
    <h2 class="">{{ title }}</h2>
    <div class="name new-title">
      {{ name }}
    </div>
    <div class="profile-main">
      {{ content }}
    </div>

    {{ if it.sub }}
    <div class="profile-sub">
      {{ it.sub }}
    </div>
    {{ /if }}
  </div>
</section>
