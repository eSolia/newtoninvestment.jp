<footer>
  <div class="inner">
    <div class="links">
      {{ for item of footer.links }}
        <a class="menu-link" href="{{ item.url }}" aria-label="Nav link in footer">{{ item.text }}</a>
      {{ /for }}
    </div>
    <div class="copy">©{{ "now" |> date("y") }} {{ footer.copy }} </div>
  </div>
</footer>

<script src="/assets/js/app.js"></script>
